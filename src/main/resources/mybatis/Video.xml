<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.Mapper.video_mapper">
	<select id="allLiveList" resultType="com.example.demo.DTO.videoDTO">
		select *
		from video
		where live_state = true
		order by video_date DESC
	</select>
	<select id="liveList" resultType="com.example.demo.DTO.videoDTO">
		select *
		from video
		where live_state = true
		order by video_date DESC
		limit 5
	</select>
	<select id="allRecordList" resultType="com.example.demo.DTO.videoDTO">
		select *
		from video
		where live_state = false
		order by video_date DESC
	</select>
	<select id="recordList" resultType="com.example.demo.DTO.videoDTO">
		select *
		from video
		where live_state = false
		order by video_date DESC
		limit 5
	</select>
	<select id="selectVideo" resultType="com.example.demo.DTO.videoDTO" parameterType="String">
		select *
		from video
		where video_code = #{video_code}
	</select>
	<insert id="addvideo">
		insert into video
		values(#{video.video_code}, #{video.streamer_id}, #{video.title}, #{video.video_date}, #{video.live_state}, #{video.save_url})
	</insert>
	<update id="changeStatus">
		update video
		set live_state = #{video.live_state},
		save_url = #{video.save_url}
		where video_code = #{video.video_code}
	</update>
</mapper>